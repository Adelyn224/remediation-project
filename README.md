# remediation-project
my final year project


# requirements
install mysql to create the connection to th database (ensure the latest version is installed)

install the dotenv library, this library loads the environment variables automatically